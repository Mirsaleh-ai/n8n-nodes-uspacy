# n8n-nodes-uspacy

An n8n community node for the Uspacy CRM and workspace API.

## Status

Version 2 is being rebuilt from scratch. It will use Uspacy Incoming Webhooks only.

## Development

```bash
npm install
npm run build
npm run lint
npm run dev
```

Never commit webhook URLs, credentials, or `.env` files.
